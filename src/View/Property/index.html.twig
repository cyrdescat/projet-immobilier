{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Properties</h1>
    <ul>
        {% for item in items %}
            <li><a href="/property/show/{{ item.id }}">{{ item.id }}. {{ item.title }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/property/add">Ajouter</a>
{% endblock %}
